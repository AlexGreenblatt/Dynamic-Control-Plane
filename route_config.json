[
  {
    "routeName": "/v1/status",
    "method": "GET",
    "responseSchema": {
      "type": "object",
      "properties": {
        "serviceId": { "type": "string" },
        "message": { "type": "string" },
        "name": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Inactive", "Maintenance"] },
        "lastUpdated": { "type": "string", "format": "date-time" }
      }
    }
  },
  {
    "routeName": "/v1/services/:serviceId/traffic",
    "method": "POST",
    "requestSchema": {
      "type": "object",
      "properties": {
        "serviceId": {
          "type": "string",
          "description": "Unique identifier for the service"
        },
        "verbose": {
          "type": "boolean",
          "description": "Flag to include detailed information in the response"
        }
      },
      "required": ["serviceId"]
    },
    "responseSchema": {
      "type": "object",
      "properties": {
        "message": { "type": "string" },
        "serviceId": { "type": "string" },
        "name": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Inactive", "Maintenance"] },
        "lastUpdated": { "type": "string", "format": "date-time" }
      }
    },
    "policies": [
      "dynamicValidation.rego"
    ]
  },
  {
    "routeName": "/v1/services/:serviceId/basicValidation",
    "method": "POST",
    "requestSchema": {
      "type": "object",
      "properties": {
        "serviceId": {
          "type": "string",
          "description": "Unique identifier for the service"
        },
        "verbose": {
          "type": "boolean",
          "description": "Flag to include detailed information in the response"
        }
      },
      "required": ["serviceId"]
    },
    "responseSchema": {
      "type": "object",
      "properties": {
        "message": { "type": "string" },
        "serviceId": { "type": "string" },
        "name": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Inactive", "Maintenance"] },
        "lastUpdated": { "type": "string", "format": "date-time" }
      }
    },
    "policies": [
      "validateServiceId.rego", "validateVerbose.rego"
    ]
  }
]
